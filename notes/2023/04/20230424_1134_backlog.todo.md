---
title: Technical debt backlog — April
slug: backlog
tags: [backlog, dev, todo]
---

# Technical debt backlog — April

## High priority

- [ ] Replace deprecated `request` library with `httpx` across three services
- [ ] Fix flaky integration test in CI (fails ~20% of runs, root cause unclear)
- [ ] Update Docker base images to address known CVEs — see [Docker security advisories](https://docs.docker.com/security/)

## Medium priority

- [ ] Add structured logging to the worker process
- [ ] Write ADR for the event-sourcing decision we made in Q1 but never documented
- [ ] Clean up dead feature flags — at least six that have been 100% on for > 3 months

## Low priority

- [ ] Migrate the old bash deployment scripts to the new tooling
- [ ] Audit and prune npm devDependencies
- [ ] Consider switching the ORM to async-compatible version

## Notes

The `request` → `httpx` migration is the most actionable. Should be doable in a focused afternoon. The flaky test needs more investigation — my current hypothesis is a race condition in the test setup, not in production code.
